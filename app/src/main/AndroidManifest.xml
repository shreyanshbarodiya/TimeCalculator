<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="shreyansh.learn.com.timecalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/a"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/a"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Admin.AdminActivities"  android:label="Activities"/>
        <activity android:name=".Admin.AdminSubactivities" android:label="Sub-Activities"/>
        <activity android:name=".User.UserActivities" android:label="Activities"/>
        <activity android:name=".User.UserSubActivities" android:label="Sub-Activities"/>
    </application>

</manifest>